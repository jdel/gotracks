{
  "name": "gotracks-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Browser checks for the behaviours jsdom cannot model.",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "playwright": "^1.62.1"
  }
}
